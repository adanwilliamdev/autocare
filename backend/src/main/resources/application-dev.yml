spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.autocare: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
