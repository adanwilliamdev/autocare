# URL base da API do backend consumida pelo frontend.
# Em desenvolvimento local, o vite.config.ts já faz proxy de /api para http://localhost:8080,
# então esta variável pode ficar em branco (usa o fallback do axios.ts).
VITE_API_URL=http://localhost:8080/api
